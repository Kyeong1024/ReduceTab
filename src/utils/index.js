import { chart } from "./chart";
import { search } from "./search";
import { onoffSwitch } from "./switch";
import { tabList, tabCount, deleteTab, controlTab } from "./tab";

export { chart, search, onoffSwitch, tabCount, tabList, deleteTab, controlTab };
